# subagents/

Canonical subagent definitions (agent-agnostic). Composed into each harness's expected location by
the profile system — e.g. `~/.claude/agents/` and each workspace's composed config `agents/`.
Ships empty; add `<name>.md` files following your harness's subagent format.
