# Remote workspace — the agent process runs on ANOTHER machine over ssh; the local pane is a view.
# Account swaps never apply (its auth lives on the remote box) and swap-fleet skips it by design.
ROOT="$HOME/work/remote-thing"
AGENTS="claude-remote"
SSH="user@remote-host"
REMOTE_CONTROL="off"
AUTOSTART="no"
