#!/usr/bin/env bash
export PATH="$HOME/.local/bin:$HOME/bin:$PATH"
if command -v opencode >/dev/null; then opencode || true; echo "[opencode exited — shell]"; else echo "opencode not installed (curl -fsSL https://opencode.ai/install | bash, then auth)"; fi; exec bash
